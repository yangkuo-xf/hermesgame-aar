<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <LinearLayout
        android:id="@+id/logoutAlertContentLinearLayout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        >
        <TextView
            android:id="@+id/tv_alert_confirm_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="start"
            android:paddingStart="24dp"
            android:textColor="@android:color/black"
            android:layout_marginTop="20dp"
            android:textSize="16dp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tv_alert_confirm_content"
            android:layout_width="wrap_content"
            android:layout_height="34dp"
            android:layout_gravity="start"
            android:layout_marginTop="13dp"
            android:gravity="top|start"
            android:paddingHorizontal="24dp"
            android:textSize="13dp"
            android:textColor="#868C92"
            android:layout_marginBottom="5dp"
            />
    </LinearLayout>

    <ImageView
        android:id="@+id/closeWindowImageView"
        android:layout_width="46dp"
        android:layout_height="46dp"
        android:src="@drawable/tds_ic_logout_close"
        android:layout_alignParentRight="true"
        android:scaleType="center"
        />
</RelativeLayout>

